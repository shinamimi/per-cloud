<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cloud.backend.mapper.ShareFileMapper">

    <insert id="insert" parameterType="com.cloud.backend.entity.ShareFile"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_share_file (share_id, file_id, parent_id, name, is_dir, size, mime_type, extension, file_hash)
        VALUES (#{shareId}, #{fileId}, #{parentId}, #{name}, #{isDir}, #{size}, #{mimeType}, #{extension}, #{fileHash})
    </insert>

    <insert id="batchInsert">
        INSERT INTO t_share_file (share_id, file_id, parent_id, name, is_dir, size, mime_type, extension, file_hash)
        VALUES
        <foreach collection="nodes" item="n" separator=",">
            (#{shareId}, #{n.fileId}, #{n.parentId}, #{n.name}, #{n.isDir}, #{n.size}, #{n.mimeType}, #{n.extension}, #{n.fileHash})
        </foreach>
    </insert>

    <select id="findByShareId" parameterType="java.lang.Long" resultType="com.cloud.backend.entity.ShareFile">
        SELECT * FROM t_share_file WHERE share_id = #{shareId}
    </select>

    <select id="findByIds" resultType="com.cloud.backend.entity.ShareFile">
        SELECT * FROM t_share_file
        WHERE share_id = #{shareId} AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <delete id="deleteByShareId" parameterType="java.lang.Long">
        DELETE FROM t_share_file WHERE share_id = #{shareId}
    </delete>

</mapper>
