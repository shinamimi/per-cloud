<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cloud.backend.mapper.FileMapper">

    <insert id="insert" parameterType="com.cloud.backend.entity.File"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_file (user_id, team_id, parent_id, name, path, size, mime_type, extension, file_hash,
                            is_directory, type, category, object_name, status)
        VALUES (#{userId}, #{teamId}, #{parentId}, #{name}, #{path}, #{size}, #{mimeType}, #{extension}, #{fileHash},
                #{isDirectory}, #{type}, #{category}, #{objectName}, #{status})
    </insert>

    <select id="findById" parameterType="java.lang.Long" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file WHERE id = #{id}
    </select>

    <select id="findByIds" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 按用户和父目录查找子文件和目录（仅 status=1 正常文件） -->
    <select id="findByUserIdAndParentId" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        WHERE user_id = #{userId} AND team_id = 0 AND parent_id = #{parentId} AND status != 0
        ORDER BY type DESC, name ASC
    </select>

    <select id="pageByUserIdAndParentId" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        WHERE user_id = #{userId} AND team_id = 0 AND parent_id = #{parentId} AND status != 0
        ORDER BY type DESC, created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByUserIdAndParentId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_file
        WHERE user_id = #{userId} AND team_id = 0 AND parent_id = #{parentId} AND status != 0
    </select>

    <!-- 同名检查（仅正常记录，回收站中的不算） -->
    <select id="findByUserIdAndParentIdAndName" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        WHERE user_id = #{userId} AND team_id = 0 AND parent_id = #{parentId} AND name = #{name} AND status != 0
        LIMIT 1
    </select>

    <!-- 按用户和路径查找（用于防止同一目录下创建同名文件） -->
    <select id="findByUserIdAndPath" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        WHERE user_id = #{userId} AND team_id = 0 AND path = #{path} AND status != 0
    </select>

    <!-- 用户全部正常文件（目录树、递归删除收集） -->
    <select id="findByUserId" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file WHERE user_id = #{userId} AND team_id = 0 AND status != 0
    </select>

    <!-- 递归查询子树（MySQL 8 CTE，替代全表扫描） -->
    <select id="findSubtree" resultType="com.cloud.backend.entity.File">
        WITH RECURSIVE subtree AS (
            SELECT * FROM t_file
            WHERE id = #{rootId} AND user_id = #{userId} AND team_id = 0 AND status != 0
            UNION ALL
            SELECT f.* FROM t_file f
            INNER JOIN subtree s ON f.parent_id = s.id
            WHERE f.user_id = #{userId} AND f.team_id = 0 AND f.status != 0
        )
        SELECT * FROM subtree
    </select>

    <update id="update" parameterType="com.cloud.backend.entity.File">
        UPDATE t_file
        SET name = #{name},
            parent_id = #{parentId},
            size = #{size},
            mime_type = #{mimeType},
            extension = #{extension},
            file_hash = #{fileHash},
            is_directory = #{isDirectory},
            type = #{type},
            category = #{category},
            object_name = #{objectName},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 重命名：仅改 name，不动其他字段 -->
    <update id="updateName">
        UPDATE t_file SET name = #{name}, updated_at = NOW() WHERE id = #{id}
    </update>

    <!-- 移动：仅改 parentId，MinIO 对象不动 -->
    <update id="updateParent">
        UPDATE t_file SET parent_id = #{parentId}, updated_at = NOW() WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM t_file WHERE id = #{id}
    </delete>

    <update id="updateStatus">
        UPDATE t_file SET status = #{status}, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="disableByHash">
        UPDATE t_file SET status = 2, updated_at = NOW()
        WHERE file_hash = #{fileHash} AND status = 1 AND is_directory = 0
    </update>

    <update id="disableByHashAndUser">
        UPDATE t_file SET status = 2, updated_at = NOW()
        WHERE file_hash = #{fileHash} AND user_id = #{userId} AND status = 1 AND is_directory = 0
    </update>

    <update id="restoreByHash">
        UPDATE t_file SET status = 1, updated_at = NOW()
        WHERE file_hash = #{fileHash} AND status = 2 AND is_directory = 0
    </update>

    <update id="updateStatusByIds">
        UPDATE t_file SET status = #{status}, updated_at = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="deleteByIds">
        DELETE FROM t_file WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findAll" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file ORDER BY created_at DESC
    </select>

    <!-- ==================== 团队维度（docs/team-module.md §四：同表 + teamId） ==================== -->

    <select id="findByTeamIdAndParentId" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        WHERE team_id = #{teamId} AND parent_id = #{parentId} AND status != 0
        ORDER BY type DESC, name ASC
    </select>

    <select id="pageByTeamIdAndParentId" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        WHERE team_id = #{teamId} AND parent_id = #{parentId} AND status != 0
        ORDER BY type DESC, created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByTeamIdAndParentId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_file
        WHERE team_id = #{teamId} AND parent_id = #{parentId} AND status != 0
    </select>

    <!-- 团队空间同名检查（同团队同目录 name 唯一；跨 user_id 共享同一命名空间） -->
    <select id="findByTeamIdAndParentIdAndName" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        WHERE team_id = #{teamId} AND parent_id = #{parentId} AND name = #{name} AND status != 0
        LIMIT 1
    </select>

    <!-- 团队全部正常文件（目录树、递归子树收集） -->
    <select id="findByTeamId" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file WHERE team_id = #{teamId} AND status != 0
    </select>

    <!-- ==================== 管理端全局文件（docs/adr/012：个人+团队统一，仅 ADMIN） ==================== -->

    <sql id="adminWhere">
        <where>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="username != null and username != ''">
                AND user_id IN (SELECT id FROM t_user
                                WHERE username LIKE CONCAT('%', #{username}, '%')
                                   OR nickname LIKE CONCAT('%', #{username}, '%'))
            </if>
            <if test="teamId != null and teamId == -1">AND team_id &gt; 0</if>
            <if test="teamId != null and teamId != -1">AND team_id = #{teamId}</if>
            <if test="category != null">AND category = #{category}</if>
            <if test="status != null">AND status = #{status}</if>
            AND status != 0
            AND is_directory = 0
        </where>
    </sql>

    <select id="adminPage" parameterType="com.cloud.backend.dto.AdminFileQuery" resultType="com.cloud.backend.entity.File">
        SELECT * FROM t_file
        <include refid="adminWhere"/>
        <choose>
            <when test="sort == 'sizeDesc'">ORDER BY size DESC, id DESC</when>
            <when test="sort == 'sizeAsc'">ORDER BY size ASC, id DESC</when>
            <otherwise>ORDER BY created_at DESC, id DESC</otherwise>
        </choose>
        LIMIT #{offset}, #{size}
    </select>

    <select id="adminCount" parameterType="com.cloud.backend.dto.AdminFileQuery" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_file
        <include refid="adminWhere"/>
    </select>

    <!-- 统计文件总数（仅正常状态） -->
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM t_file WHERE status = 1
    </select>

    <!-- 统计文件总大小（仅正常状态） -->
    <select id="sumSize" resultType="long">
        SELECT COALESCE(SUM(size), 0) FROM t_file WHERE status = 1
    </select>

</mapper>
