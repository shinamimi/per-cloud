<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cloud.backend.mapper.UserMapper">

    <!-- useGeneratedKeys + keyProperty：插入后自动回写自增 ID -->
    <insert id="insert" parameterType="com.cloud.backend.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, email, nickname, avatar, role, quota, used_space, status,
                            is_vip, admin_bonus_quota, reward_quota)
        VALUES (#{username}, #{password}, #{email}, #{nickname}, #{avatar}, #{role}, #{quota}, #{usedSpace}, #{status},
                #{isVip}, #{adminBonusQuota}, #{rewardQuota})
    </insert>

    <select id="findById" parameterType="java.lang.Long" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user WHERE id = #{id}
    </select>

    <select id="findByIds" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findByUsername" parameterType="java.lang.String" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user WHERE username = #{username}
    </select>

    <select id="findByEmail" parameterType="java.lang.String" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user WHERE email = #{email}
    </select>

    <!-- 支持用户名或邮箱登录 -->

    <select id="findByAccount" parameterType="java.lang.String" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user WHERE username = #{account} OR email = #{account} LIMIT 1
    </select>

    <select id="findAll" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user ORDER BY created_at DESC
    </select>

    <!-- updated_at = NOW()：每次更新自动更新时间戳 -->
    <update id="update" parameterType="com.cloud.backend.entity.User">
        UPDATE t_user
        SET password = #{password},
            nickname = #{nickname},
            avatar = #{avatar},
            role = #{role},
            quota = #{quota},
            used_space = #{usedSpace},
            status = #{status},
            is_vip = #{isVip},
            admin_bonus_quota = #{adminBonusQuota},
            reward_quota = #{rewardQuota},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

    <!-- 原子调整已用空间：used_space = used_space + delta，避免并发覆盖；负数钳制为 0 -->
    <update id="updateUsedSpace">
        UPDATE t_user SET used_space = GREATEST(used_space + #{delta}, 0) WHERE id = #{userId}
    </update>

    <!--
      按注册日期范围 + 角色/状态过滤查询（quota-batch 预览与执行共用）。
      role/status 枚举存 ordinal（USER=0, VIP 用 is_vip 区分；NORMAL=1, DISABLED=0, LOCKED=2, INACTIVE=3）。
    -->
    <select id="findByQuotaFilter" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user
        <where>
            <if test="startDate != null">AND DATE(created_at) &gt;= #{startDate}</if>
            <if test="endDate != null">AND DATE(created_at) &lt;= #{endDate}</if>
            <choose>
                <when test="role == 'USER'">AND role = 0 AND (is_vip = 0 OR is_vip IS NULL)</when>
                <when test="role == 'VIP'">AND is_vip = 1</when>
            </choose>
            <choose>
                <when test="status == 'NORMAL'">AND status = 1</when>
                <when test="status == 'DISABLED'">AND status = 0</when>
                <when test="status == 'LOCKED'">AND status = 2</when>
                <when test="status == 'INACTIVE'">AND status = 3</when>
            </choose>
        </where>
        ORDER BY created_at DESC
    </select>

    <!-- 批量更新配额（quota-batch 执行） -->
    <update id="batchUpdateQuota">
        UPDATE t_user SET quota = #{quota}, updated_at = NOW() WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>

    <!-- 用户搜索：用户名/邮箱前缀模糊（手机号字段预留，本期未绑定） -->
    <select id="searchByKeyword" resultType="com.cloud.backend.entity.User">
        SELECT * FROM t_user
        WHERE status = 1
          AND (username LIKE CONCAT(#{keyword}, '%') OR email LIKE CONCAT(#{keyword}, '%'))
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>