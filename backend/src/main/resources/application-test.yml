spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_test?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      database: 1

  mail:
    host: ${SMTP_HOST:localhost}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:test@test.dev}
    password: ${SMTP_PASSWORD:test}
    from: ${SMTP_FROM:test@test.dev}

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: cloud-test

jwt:
  secret: kF5Wih0b8zNo4/UsEFR5yN8y5OfsHArdLr6xCSKfKTo=
  expiration: 86400000
  header: Authorization
  prefix: Bearer
  issuer: cloud

redis:
  host: localhost
  port: 6379
  database: 1
  password:
  timeout: 3000

file:
  storage-path: /tmp/cloud-storage-test
  chunk-size: 10485760
  max-size: 536870912
